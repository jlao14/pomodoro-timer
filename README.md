# pomodoro-timer
A classic, graphical Pomodoro timer application built using Python and the native `tkinter` library. This tool helps you manage your time and improve productivity by breaking work down into focused intervals separated by short breaks.

## Features
- **Standard Pomodoro Intervals:** Automatically cycles through 25-minute work sessions, 5-minute short breaks, and a 20-minute long break after four completed work sessions.
  
- **Visual Tracking:** Displays a clean countdown timer and tracks your completed work sessions with checkmarks (✓) at the bottom of the window.
  
- **Custom UI:** A focused, distraction-free interface styled with custom colors and a central tomato graphic.
  
- **Reset Functionality:** Easily reset the timer and your session progress at any point with a single click.

## Prerequisites
To run this application, you only need Python installed on your system. Both `tkinter` and `math` are built into Python's standard library.

## Setup & Installation
1. Clone this repository or download the project files to your local machine.
2. Ensure that both `main.py` and the `tomato.png` image file are located in the exact same directory.

## How to Use
1. Open your terminal or command prompt.
2. Navigate to the folder containing the project files.
3. Run the application:
   ```bash
   python main.py
